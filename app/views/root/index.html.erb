<div class="col-md-offset-2 col-md-8">
  <div class="book-header <%= current_user? ? '' : 'logged' %>">
    <h1>ム mumuki<%= Organization.current.title_suffix %></h1>

    <img src="<%= Organization.current.banner_url %>" alt="<%= Organization.current.name %> - Mumuki">
    <h2><%= Organization.current.name %></h2>
  </div>

  <div class="text-box">
    <%= Organization.current.description %>
  </div>

  <% unless @user_organizations.empty? %>
      <h2><%= t(:private) %></h2>

      <%= render partial: 'root/organizations', locals: {organizations: @user_organizations} %>

      <h2><%= t(:public) %></h2>
  <% end %>

  <%= render partial: 'root/organizations', locals: {organizations: @public_organizations} %>
</div>
